# Production 3D Model Folder — Acer Nitro ANV15-41

Place the authoritative production `nitro-anv15-41.glb` file here:

`public/models/nitro-anv15-41.glb`

When present, `LaptopModel.tsx` and `partRegistry.ts` automatically detect, normalize, and render the production GLB asset while preserving all camera presets, master scroll story timeline keyframes, and part highlight modes (`fanHighlight`, `keyboardHighlight`, `portHighlight`).
